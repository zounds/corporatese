import React from "react";
import ReactDOM from "react-dom/client";
import CorporateseGame from "./App.jsx";

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <CorporateseGame />
  </React.StrictMode>
);
